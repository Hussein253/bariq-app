# نظام التشغيل والتعليمات الهندسية: منصة الأتمتة اللوجستية (شركة المندوب للتوصيل السريع)

## 1. نظرة عامة والهدف العام (Project Overview & Vision)

- **المؤسسة:** شركة "المندوب للتوصيل السريع" (خدمة التوصيل الفوري بنفس اليوم من التاجر إلى كافة محافظات العراق).

- **اسم المشروع/المنصة:** "برق" - منصة الأتمتة والربط اللوجستي الذكي.

- **الهدف الجوهري للمنصة:**

  بناء نظام بيئي مؤتمت عالي الدقة يربط قنوات استقبال الطلبات والمراسلات (Meta API / WhatsApp Cloud API) بنظام إدارة الطلبات (OMS) والواجهات البرمجية (APIs) لشركة التوصيل وقواعد البيانات المركزية، لإدارة دورة حياة الشحنة كاملة ومطابقة الكشوفات المالية والحسابية بدقة متناهية ودون تدخل يدوي.

---

## 2. القواعد الصارمة للتنفيذ والتطوير (Engineering Directives)

### أ. قاعدة انعدام التخمين (Zero Hallucination Policy)

- يُحظر حظرًا تامًا تخمين أو افتراض أي معرّفات (IDs)، مفاتيح استجابة (API Keys/Fields)، مبالغ مالية، أو أسماء أعمدة في قاعدة البيانات.

- في حال غياب أي تفصيلة تخص استجابة الـ API لشركة الشحن أو شروط التحقق (Validation Rules)، يجب التوقف وطرح سؤال مباشر ومحدد بدلاً من كتابة كود افتراضي.

- التعامل مع الشحنات يعامل معاملة الأنظمة المالية الحساسة: الخطأ في معرف الشحنة أو حالتها المالية غير مقبول برمجياً.

### ب. الهندسة أولاً (Schema-First Development)

- يُمنع توليد كود منطق الأعمال (Business Logic)، أو استعلامات SQL، أو مسارات عمل (Workflows) دون اعتماد هيكل البيانات أولاً.

- الترتيب الإلزامي لكل ميزة برمجية:

  1. تقديم وتوثيق هيكل البيانات المتوقع `JSON Schema` / `PostgreSQL DDL` / `TypeScript Interfaces`).

  2. اعتماد المخطط والتأكد من مطابقة الحقول المالية واللوجستية.

  3. كتابة الاستعلامات والتنفيذ البرمجي.

### ج. التطوير المرحلي والتجزيء (Step-by-Step Architecture)

- تقسيم بناء المسارات البرمجية إلى وحدات معزولة وسهلة الاختبار (Modular Design).

- عدم تقديم مسارات n8n المعقدة أو نصوص الأكواد دفعة واحدة، بل يتم إنجاز وتثبيت كل مرحلة على حدة:

  - التحقق من الـ Webhook واستقبال البيانات.

  - معالجة البيانات والتحقق من صحتها (Validation & Idempotency).

  - التخزين وتحديث الحالات.

  - استدعاء الـ APIs الخارجية ومعالجة الأخطاء (Error Handling & Retries).

---

## 3. المكدس التقني المعتمد (Technical Stack)

- **محرك الأتمتة وسير العمل:** n8n.

- **قاعدة البيانات والتخزين:** Supabase (PostgreSQL) مع الالتزام بتفعيل قيود المفاتيح الأجنبية، الفهارس الذكية، وسياسات الأمان (RLS).

- **واجهات التواصل الخارجي:** Meta Graph API (WhatsApp Business API / Messenger / Instagram).

- **أدوات بيئة العمل والمراجعة البرمجية:** Claude Code و Claude Cowork.

---

## 4. المعايير اللوجستية والمالية القياسية (Logistics Standards)

1. **ضمان عدم التكرار (Idempotency):**

   - يجب أن تحتوي كل عملية إنشاء طلب على `idempotency_key` أو فحص دقيق لرقم تتبع الشحنة لمنع تكرار قيد الطلبات في حال إعادة إرسال الـ Webhook.

2. **آلة حالات الشحنة (Shipment State Machine):**

   - حالات الشحنة يجب أن تتبع تدفقاً صارماً وغير قابل للقفز العشوائي:

     `ORDER_RECEIVED` -> `PICKED_UP_SAME_DAY` -> `IN_TRANSIT_HUB` -> `OUT_FOR_DELIVERY` -> `DELIVERED` | `POSTPONED` | `RETURNED`) -> `SETTLED_FINANCIALLY`.

3. **التوزيع الجغرافي العراقي:**

   - ضبط حقول العنوان بشكل معياري: المحافظة، القضاء/الناحية، أقرب نقطة دالة، رقم هاتف المستلم (مع التحقق من صيغ أرقام شركات الاتصال المحلية مثل Zain, Asiacell, Korek).

4. **المطابقة المالية (Financial Reconciliation):**

   - فصل سعر البضاعة (COD Amount) عن أجور التوصيل الصافية (Delivery Fee)، وتضمين حالات تسوية واضحة لكشوفات التجار المودعة والمؤجلة.

---

## 5. قواعد صياغة ردود البوت وخدمة العملاء (Customer-Facing Rules)

- **النبرة والأسلوب:** احترافية، مهنية، واضحة، ومهذبة، تتناسب بدقة مع بيئة التجارة الإلكترونية والسوق العراقي.

- **وضوح الرسالة:** الردود يجب أن تكون مباشرة ومفيدة (تجنب الإطالة الإنشائية غير المفيدة)، وتحتوي على التفاصيل الحيوية فقط: رقم التتبع، المبلغ الكلي بالدينار العراقي (IQD)، وحالة الشحنة الفعلية.

- **التعامل مع المشكلات:** صياغة رسائل التأجيل أو تصحيح العناوين بلهجة خدمة عملاء راقية تدعم سرعة حسم المشكلة بين المندوب والزبون والتاجر.