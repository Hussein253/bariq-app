-- ============================================================================
-- تهيئة أولى: منح أول حساب دور مالك المنصة
-- ============================================================================
--
-- ⚠️ لم يعد لازماً لتشغيل المنصة. التطبيق يمنح دور المالك ذاتياً عند أول
-- دخول لبريد المالك (lib/platform-owner.ts + lib/merchant-onboarding.ts):
-- بريد المالك → platform_owner، وأي بريد آخر → merchant على باقة Spark.
-- فلا حاجة لإنشاء الحساب من لوحة Supabase ولا لتشغيل شيء قبل أول دخول.
--
-- يبقى هذا الملف لحالتين يدويتين فقط:
--   • منح دور staff أو merchant لحساب قائم (القسمان ٢ و٣ أدناه)
--   • تصحيح دور مُنح خطأً — القائمة في الكود تُقرأ عند **الإنشاء** فقط،
--     فمن دخل مشتركاً قبل إضافة بريده إليها يبقى مشتركاً حتى يُعدَّل هنا.
--
-- ⚠️ ليس ترحيلاً: هوية المالك قرار تشغيلي يخصّ هذا النشر، ولا يصح أن
-- يُعاد تطبيقه على كل بيئة.
--
-- كيف يُشغَّل: من Supabase Dashboard → SQL Editor، بعد استبدال البريد أدناه.
-- ============================================================================

-- ----------------------------------------------------------------------------
-- 1) مالك المنصة — للتصحيح فقط، لا للتهيئة
-- ----------------------------------------------------------------------------
-- التطبيق يمنح هذا الدور ذاتياً لصاحب البريد أدناه عند أول دخول. لا يُشغَّل
-- هذا القسم إلا لتصحيح حساب دخل قبل ضبط القائمة فصار مشتركاً — وعندها يُحذف
-- ربطه بتاجره أولاً، لأن قيد profiles_merchant_link_check يمنع مالكاً
-- بـ merchant_id (الجملة أدناه تصفّره).

insert into public.profiles (user_id, role, merchant_id, store_name)
select u.id, 'platform_owner', null, 'برق'
from auth.users u
where u.email = 'hus4561990@gmail.com'
on conflict (user_id) do update
  set role = 'platform_owner',
      merchant_id = null,
      updated_at = now();

-- ----------------------------------------------------------------------------
-- 2) موظف برق (اختياري)
-- ----------------------------------------------------------------------------
-- يرى /operations و /operations/chats و /dashboard، ولا يرى /admin
-- (أرصدة التجار والإيرادات والعمولات).
--
-- insert into public.profiles (user_id, role, merchant_id, store_name)
-- select u.id, 'staff', null, null
-- from auth.users u
-- where u.email = 'staff@example.com'
-- on conflict (user_id) do update set role = 'staff', merchant_id = null;

-- ----------------------------------------------------------------------------
-- 3) تاجر (اختياري)
-- ----------------------------------------------------------------------------
-- merchant_id إلزامي هنا: قيد profiles_merchant_link_check يرفض تاجراً بلا
-- تاجر مرتبط، لأن حساباً كهذا يدخل ولا يرى شيئاً.
--
-- insert into public.profiles (user_id, role, merchant_id, store_name)
-- select u.id, 'merchant', 'ضع-معرّف-التاجر-هنا'::uuid, 'اسم المتجر'
-- from auth.users u
-- where u.email = 'merchant@example.com'
-- on conflict (user_id) do update
--   set role = 'merchant', merchant_id = excluded.merchant_id;

-- ----------------------------------------------------------------------------
-- التحقق بعد التشغيل
-- ----------------------------------------------------------------------------
-- select u.email, p.role, p.merchant_id
-- from public.profiles p join auth.users u on u.id = p.user_id
-- order by p.role;
