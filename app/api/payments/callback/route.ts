import { paymentsNotImplemented } from '@/lib/payments'

/**
 * /api/payments/callback — إشعار نتيجة الدفع من البوابة
 * ======================================================
 * ⚠️ ثغرة أُغلقت هنا: كان هذا المسار يقبل GET ويكتب "تم الدفع" لأي طلب
 * يُمرَّر معرّفه في الرابط — بلا توقيع ولا تحقق من المبلغ ولا سؤال للبوابة.
 * أي شخص يفتح ‎/api/payments/callback?order_id=123‎ كان يُسقط ثمن الطلب.
 *
 * القواعد الملزمة لأي تنفيذ مستقبلي لهذا المسار:
 *   1. POST فقط. رجوع المتصفح (GET) لا يُعدّ إثبات دفع أبداً — الزبون يتحكم به.
 *   2. التحقق من توقيع البوابة على الجسم الخام قبل أي قراءة له
 *      (lib/webhook-signature.ts).
 *   3. سؤال البوابة عن حالة المعاملة (server-to-server) وعدم تصديق الجسم وحده.
 *   4. مطابقة المبلغ المُحصَّل بمبلغ الطلب المخزَّن قبل أي تحديث.
 *   5. منع التكرار بمفتاح المعاملة — إشعار مكرر يجب ألا يُرحّل المبلغ مرتين.
 *
 * حتى ذلك الحين: ٥٠١ في الحالتين، ولا كتابة في قاعدة البيانات إطلاقاً.
 */
export async function POST() {
  return paymentsNotImplemented()
}

export async function GET() {
  return paymentsNotImplemented()
}
